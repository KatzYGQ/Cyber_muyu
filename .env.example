# Supabase (insforge backend)
# Get URL from insforge backend metadata; use service_role key for server-side write.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Or for client-only: use anon key (requires RLS allowing update on cyber_muyu)
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
